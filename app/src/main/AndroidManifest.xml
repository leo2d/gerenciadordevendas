<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leonardo.gerenciadordevendas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ListaVendasActivity"
            android:label="@string/title_activity_lista_vendas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TelaCadastroDeVendasActivity" />
        <activity android:name=".TelaListaProdutoActivity" />
        <activity android:name=".TelaCadastroProdutoActivity" />
        <activity android:name=".TelaProdutosActivity" />
        <activity android:name=".ListaDeClientesActivity" />
        <activity android:name=".CadastroDeClientesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>